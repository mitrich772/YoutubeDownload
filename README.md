# 🎬 YouTube Downloader

Десктопное приложение для скачивания видео и аудио с YouTube.  
Построено на **PyQt6** + **yt-dlp**, с тёмной темой и поддержкой DASH (1080p+).

---

## ✨ Возможности

- 📥 **Скачивание видео** в любом доступном разрешении (360p – 4K)
- 🎵 **Извлечение аудио** в MP3 (при наличии FFmpeg)
- 🔀 **Автоматическое склеивание** видео + аудио через FFmpeg (DASH-форматы)
- 📋 **Smart Paste** — автоподхват YouTube-ссылки из буфера обмена
- 📦 **Очередь загрузок** с прогресс-барами, скоростью и ETA
- ⚙ **Настройки** — папка сохранения, качество, макс. загрузок, лог
- 🌑 **Тёмная тема** в стиле YouTube

---


---

## 🚀 Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/mitrich772/YoutubeDownload.git
cd YoutubeDownload
```

### 2. Создайте виртуальное окружение

```bash
python -m venv .venv

# Windows
.venv\Scripts\activate

# macOS / Linux
source .venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. (Рекомендуется) Установите FFmpeg

FFmpeg нужен для:
- Скачивания видео в 1080p и выше (DASH-форматы)
- Конвертации аудио в MP3

**Windows:** скачайте с [ffmpeg.org](https://ffmpeg.org/download.html) и поместите `ffmpeg.exe` / `ffprobe.exe` в папку с приложением или добавьте в PATH.

**macOS:**
```bash
brew install ffmpeg
```

**Linux:**
```bash
sudo apt install ffmpeg
```

> Приложение автоматически найдёт FFmpeg рядом с собой или в PATH.

---

## ▶ Запуск

```bash
python main.py
```

---

## 📁 Структура проекта

```
YoutubeDownload/
├── main.py                  # Точка входа
├── requirements.txt         # Зависимости
│
├── core/
│   ├── config.py            # Центральный конфиг приложения
│   ├── converter.py         # Поиск FFmpeg
│   ├── downloader.py        # Загрузка видео/аудио (QThread)
│   ├── logger.py            # Логирование (с переключалкой)
│   └── parser.py            # Парсинг метаданных YouTube
│
└── ui/
    ├── download_card.py     # Карточка загрузки
    ├── main_window.py       # Главное окно
    ├── settings_dialog.py   # Диалог настроек
    └── styles.py            # QSS-стили (Dark Mode)
```

---

## ⚙ Конфигурация

Параметры по умолчанию находятся в [`core/config.py`](core/config.py):

| Параметр              | Значение по умолчанию              | Описание                         |
|-----------------------|------------------------------------|----------------------------------|
| `DEFAULT_OUTPUT_DIR`  | `~/Downloads`                      | Папка сохранения                 |
| `DEFAULT_FORMAT`      | `1080p`                            | Качество видео                   |
| `DEFAULT_MAX_CONCURRENT` | `3`                             | Макс. одновременных загрузок     |
| `DEFAULT_SMART_PASTE` | `False`                            | Автоподхват ссылки из буфера     |
| `DEFAULT_LOG_TO_FILE` | `True`                             | Запись лога в файл               |

Пользовательские настройки сохраняются в `~/.youtube_downloader_settings.json`.

---

## 📝 Логирование

Лог записывается в файл `youtube_downloader.log` рядом с приложением.  
Запись лога можно отключить в **⚙ Настройки → Записывать лог в файл**.

---

## 🛠 Зависимости

| Пакет    | Версия   | Назначение                    |
|----------|----------|-------------------------------|
| yt-dlp   | ≥ 2024.12 | Скачивание с YouTube         |
| PyQt6    | ≥ 6.6     | Графический интерфейс        |
| FFmpeg   | опционально | Склеивание DASH, конвертация |

---

## 📄 Лицензия

MIT License

## 🤝 Contributing (Как предложить код)

Если вы хотите добавить новую фичу или исправить баг через Pull Request, пожалуйста, убедитесь, что ваш код проходит локальные тесты:

1. Установите зависимости для разработки:
   ```bash
   pip install -r requirements-dev.txt
   ```
2. Запустите тесты:
   ```bash
   pytest tests/ -v
   ```
3. Убедитесь, что все тесты зелёные (✅ `PASSED`).
4. После создания PR, GitHub Actions также автоматически проверит ваш код.

---

> Сделано с ❤️ и Python
